# Rust
/target

# Node
frontend/node_modules

# 前端构建产物（保留 .gitkeep 占位，rust-embed 编译期需要目录存在）
frontend/dist/*
!frontend/dist/.gitkeep

# 运行数据（sqlite 数据库文件）
/data

# 本地运行配置（模板见 config.example.yaml）
/config.yaml

# 日志与临时文件
*.log
.DS_Store
